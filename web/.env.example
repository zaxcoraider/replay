# Copy to .env.local for local dev, or set in Vercel project settings for production.

# URL of the deployed replay-api (e.g. https://replay-api.fly.dev)
# Defaults to http://localhost:8787 when unset (local dev).
NEXT_PUBLIC_REPLAY_API_URL=
